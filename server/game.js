const { GRID_SIZE } = require('./constants');

module.exports = {
    initGame,
  }
  
function initGame() { 
    const state = {
        players: [{
            score: 0,
        }, {
            score: 0,
        }],
        // TODO: add the board data

    }

}